## Rules

1. **On every session start** — read `DECISIONS.md` to understand prior decisions and context.
2. **Before every code action** — write the planned next steps in `DECISIONS.md`.
3. **After every code action** — write progress/results in `DECISIONS.md`.
4. **Server lifecycle** — if I start the backend for dev (`cd back && npm run start` from this project), **stop it when the task is done** — the user wants to run it themselves from their own console to see output.

---

## Guidelines
- Prefer editing existing files over creating new ones.
- Run lint/typecheck after code changes.
- Do not commit unless explicitly asked.
- Follow existing code conventions and style.